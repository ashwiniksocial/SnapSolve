STOP.

Do not build any new features.

Do not build any new engines.

Do not build any analytics.

Do not build any dashboards.

Do not build any recommendation systems.

We are now debugging the teaching pipeline.

The objective is to prove that the Practice page is rendering the new TeachingLesson object and not the legacy steps[] renderer.

I want an end-to-end audit.

For one Practice question and one scanned homework question:

1. Log the exact backend response.

2. Save the raw JSON before any mapping.

3. Log the object after openaiSolver mapping.

4. Log the object received by the frontend.

5. Log which renderer is selected.

6. Log whether LessonRenderer or LegacyRenderer is used.

7. Log every condition that causes fallback.

8. Show screenshots proving which renderer is displayed.

9. If TeachingLesson exists but LegacyRenderer is chosen, remove that logic.

10. If TeachingLesson reaches the frontend but sections are hidden, identify exactly why.

11. If the backend is still returning steps[] instead of TeachingLesson, fix the backend immediately.

12. The Practice page must ALWAYS render the complete Teaching Lesson whenever lesson data exists.

The lesson shown to the student must include every section generated by the backend.

No truncation.

No collapsing into the old UI.

No conversion back into simple solution steps.

No silent fallback.

No hidden sections.

No loss of reasoning.

The Practice page should visibly become a teaching lesson rather than a solution card.

Do not stop after code changes.

Run the application.

Solve multiple questions.

Provide screenshots proving that:

- Before We Start
- Prerequisites
- Vocabulary
- Concept Teaching
- Why Students Get Confused
- Guided Reasoning
- Every Why
- Every Calculation
- Pause & Think
- Similar Example
- Check Understanding
- Practice Question
- Final Answer

are all visible in the Practice page.

The task is complete only when the screenshots clearly demonstrate that the Practice page has transformed from a simple answer card into a complete classroom-style lesson.